EXCEPTIONS
    OTHERS        = 1.

IF sy-subrc <> 0.
  <ls_output_row>-msg = |BAPI failed with sy-subrc { sy-subrc }|.
ELSE.
  LOOP AT lt_return INTO ls_return WHERE type = 'E' OR type = 'A'.
    <ls_output_row>-msg = ls_return-message.
  ENDLOOP.
ENDIF.